<UserControl x:Class="HCL_ODA_TestPAD.Views.AppMenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HCL_ODA_TestPAD.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Menu>
            <MenuItem Header="File">
                <!--<MenuItem Header="Load Fibex (.xml)" Command="{Binding LoadFibexCommand}"/>
                <Separator />-->
                <!--<MenuItem Header="Load Data Model (.db)" Command="{Binding LoadDataModelCommand}"/>-->
                <MenuItem Header="{Binding MergeAndSaveMenuName}" Command="{Binding MergeAndSaveDataModelCommand}"/>
                <!--<Separator />
                <MenuItem Header="Save Data Model (.db)" Command="{Binding SaveDataModelCommand}"/>-->
            </MenuItem>
            <MenuItem Header="Edit"/>
            <MenuItem Header="View">
                <MenuItem Header="Data Model (.htm)" Command="{Binding ViewDataModelCommand}"/>
            </MenuItem>

            <MenuItem Header="About"/>
        </Menu>

    </Grid>
</UserControl>
